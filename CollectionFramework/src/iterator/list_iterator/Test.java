package iterator.list_iterator;

public class Test {
	/*
	 * Both Iterator and ListIterator are used to iterate through elements of a
	 * collection class. Using Iterator we can traverse in one direction
	 * (forward) while using ListIterator we can traverse the collection class
	 * on both the directions(backward and forward)
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
