package collection.map;

public class Test {
	/*
	 * A map is a container that stores the elements along with the keys. The
	 * keys are like indexes. In List, the indexes are integers. In Map, the
	 * keys can be any objects. A map cannot contain duplicate keys. Each key
	 * maps to one value. A key and its corresponding value from an entry, which
	 * is actually stored in a map. HashMap, HashTable,TreeMap and LinkedHashMap
	 * are classes implementing Map interface.
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
