<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tarkeshwar.charts">
<uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GraphTopup" />
        <activity android:name=".FoundationSkills" />
        <activity android:name=".TOPUPDivision" />
        <activity android:name=".ITESFSFinal" />
        <activity android:name=".GraphPlacement" />
        <activity android:name=".GraphOverall" />
        <activity android:name=".GraphFinalAssessmentTopup" />
        <activity android:name=".GraphFinalAssessmentFS" />
        <activity android:name=".GraphDivisionTopup" />
        <activity android:name=".GraphDivisionOverAll" />
        <activity android:name=".GraphDivisionFS" />
    </application>

</manifest>